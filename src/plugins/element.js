import Vue from "vue";
import { Button, Select, Switch } from "element-ui";

// Vue.use(Button, Radio);
Vue.component(Button.name, Button);
Vue.component(Select.name, Select);
Vue.component(Switch.name, Switch);
